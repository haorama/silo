{
  "name": "@haorama/silo",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "author": "Michael Lazuardy",
  "keywords": [
    "filesystem",
    "storage",
    "nodejs storage",
    "s3"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mlazuardy/silo"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rimraf dist && tsc --build tsconfig.json",
    "dev": "tsc --build tsconfig.json --watch",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.13.0",
    "eslint-config-custom": "workspace:*",
    "rimraf": "^5.0.7",
    "tsconfig": "workspace:*",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
